base-config: &base_config
  # Development RLZ brand code inherited from ChromiumOS generic bring-up.
  brand-code: ZZCR
  identity:
    platform-name: Grom
  name: grom-amd64

chromeos:
  devices:
    - skus:
        - config: *base_config
